# SapMachine 21.0.10.0.1 demo

This repo demonstrates using `actions/setup-java` with:

- `distribution: sapmachine`
- Java version `21.0.10.0.1` (represented as `21.0.10+0.1` for matching)

Run the GitHub Actions workflow to see `java -version` output.
